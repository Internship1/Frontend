'use strict';

describe('myApp.loginpage module', function() {

  beforeEach(module('myApp.loginpage'));

  describe('loginpage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var loginpageCtrl = $controller('LoginpageCtrl');
      expect(loginpageCtrl).toBeDefined();
    }));

  });
});