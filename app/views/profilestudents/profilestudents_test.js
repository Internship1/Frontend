'use strict';

describe('myApp.profilestudents module', function() {

  beforeEach(module('myApp.profilestudents'));

  describe('profilestudents controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var profilestudentsCtrl = $controller('ProfileStudentsCtrl');
      expect(profilestudentsCtrl).toBeDefined();
    }));

  });
});