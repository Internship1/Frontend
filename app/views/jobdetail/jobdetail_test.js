'use strict';

describe('myApp.jobdetail module', function() {

  beforeEach(module('myApp.jobdetail'));

  describe('jobdetail controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var jobdetailCtrl = $controller('JobdetailCtrl');
      expect(jobdetailCtrl).toBeDefined();
    }));

  });
});