'use strict';

describe('myApp.faqs module', function() {

  beforeEach(module('myApp.faqs'));

  describe('faqs controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var faqsCtrl = $controller('faqsCtrl');
      expect(faqsCtrl).toBeDefined();
    }));

  });
});