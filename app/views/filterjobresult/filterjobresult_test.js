'use strict';

describe('myApp.filterjobresult module', function() {

  beforeEach(module('myApp.filterjobresult'));

  describe('filterjobresult controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var filterjobresultCtrl = $controller('FilterjobresultCtrl');
      expect(filterjobresultCtrl).toBeDefined();
    }));

  });
});