'use strict';

describe('myApp.profilepage module', function() {

  beforeEach(module('myApp.profilepage'));

  describe('profilepage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var profilepageCtrl = $controller('ProfilepageCtrl');
      expect(profilepageCtrl).toBeDefined();
    }));

  });
});