'use strict';

describe('myApp.addmorejob module', function() {

  beforeEach(module('myApp.addmorejob'));

  describe('addmorejob controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var addmorejobCtrl = $controller('AddmorejobCtrl');
      expect(addmorejobCtrl).toBeDefined();
    }));

  });
});