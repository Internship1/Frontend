'use strict';

describe('myApp.Composemail module', function() {

  beforeEach(module('myApp.composemail'));

  describe('composemail controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var composemailCtrl = $controller('ComposemailCtrl');
      expect(composemailCtrl).toBeDefined();
    }));

  });
});