'use strict';

describe('myApp.editprofile module', function() {

  beforeEach(module('myApp.editprofile'));

  describe('editprofile controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var editprofileCtrl = $controller('EditprofileCtrl');
      expect(editprofileCtrl).toBeDefined();
    }));

  });
});