'use strict';

describe('myApp.mailbox module', function() {

  beforeEach(module('myApp.mailbox'));

  describe('mailbox controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mailboxCtrl = $controller('MailboxCtrl');
      expect(mailboxCtrl).toBeDefined();
    }));

  });
});