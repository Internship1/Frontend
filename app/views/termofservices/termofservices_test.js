'use strict';

describe('myApp.termofservices module', function() {

  beforeEach(module('myApp.termofservices'));

  describe('termofservices controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var termofservicesCtrl = $controller('TermofservicesCtrl');
      expect(termofservicesCtrl).toBeDefined();
    }));

  });
});